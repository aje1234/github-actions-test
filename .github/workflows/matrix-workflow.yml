name: Matrix Workflow

on: 
   workflow_dispatch:

jobs:
   node-matrix:
      strategy:
         matrix:
            os: [ubuntu-latest, windows-latest]
            node-version: [14, 15]
      runs-on: ${{ matrix.os }}
      steps:
         - run: echo '123'
